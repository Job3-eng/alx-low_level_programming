#include "main.h"
#include <stdio.h>
/**
 * main - Printing te name of program
 * @argc: total number of arguments on the command line
 * @argv: Array with a argc size
 * Return: 0 (successful)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
